# @package _global_
defaults:
  - override /agent: td3
  - override /environment: pendulum
  - _self_

# TD3 on Pendulum-v1 — ~1M env steps
trainer:
  max_epochs: 1000
  fast_dev_run: false

env:
  num_batches_per_epoch: 1000
  buffer_size: 1000000

agent:
  learning_starts: 5000
  batch_size: 256
