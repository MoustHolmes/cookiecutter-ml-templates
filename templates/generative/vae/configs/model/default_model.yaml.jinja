_target_: {{repo_name}}.vae_module.VAEModule

latent_dim: 128

encoder:
  _target_: {{repo_name}}.models.encoder.Encoder
  in_channels: 1
  image_size: [28, 28]
  latent_dim: 128

decoder:
  _target_: {{repo_name}}.models.decoder.Decoder
  out_channels: 1
  image_size: [28, 28]
  latent_dim: 128
  output_activation:
    _target_: torch.nn.Sigmoid

optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 1.0e-3

beta: 1.0
kl_warmup_epochs: 0
